Place fort image files here (exact filenames as stored in MongoDB, e.g. 1777812957255-RamshejFort4.jpg).
On server start they are copied to uploads/images/ so Render can serve /images/<filename>.

If files are missing, the API falls back to Pexels URLs from fortCatalog for seeded forts.
