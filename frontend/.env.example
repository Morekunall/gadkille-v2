# --- API (pick one strategy) ---

# REQUIRED for Google sign-in (OAuth callback is on Render):
VITE_API_URL=https://gadkille-backend-clean.onrender.com/api

# Local backend only (email/password via Vite proxy — Google will NOT work):
# VITE_API_URL=

# Optional: override OAuth host if different from VITE_API_URL
# VITE_OAUTH_API_ORIGIN=https://gadkille-backend-clean.onrender.com

VITE_GOOGLE_MAPS_API_KEY=your_key_here
